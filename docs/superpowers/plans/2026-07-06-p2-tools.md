# P2 本地执行能力 + 工具系统 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让启明 Agent 具备本地工具调用能力——LLM 可调用 6 个工具（读/列/搜/写/编/执行 shell），危险操作经古风审批弹窗逐次放行，AI SDK 原生 tool-use 循环最多 25 轮。

**Architecture:** Main 进程新增 `tools/`（注册表 + 路径解析 + 6 个内置工具）与 `approval/`（审批队列，sessionId 隔离，pending Promise 挂起）。`chat/run.ts` 从 `result.textStream` 改为 `result.fullStream`，给 `streamText` 传 `tools` + `maxSteps: 25`，由 SDK 自动驱动 tool-use 循环。Renderer 新增审批弹窗（朱砂印意象）与工具调用折叠卡片，chat store 扩展 toolCalls 状态。

**Tech Stack:** Electron 30 + electron-vite + React 18 + TypeScript + Vite + zustand + Tailwind + 古风令牌（`@qiming/design-tokens`）+ Vercel AI SDK `ai@4.3.19` + keytar + better-sqlite3 + pnpm workspace。**P2 新增依赖：`zod`（工具参数 schema，AI SDK 用它生成 function calling schema + 运行时校验）。**

## Global Constraints

- **古风令牌强制**：所有颜色/字体/间距/动效必须走 `var(--*)`（来源 `@qiming/design-tokens`），零硬编码十六进制（CSS 纹理 rgba 例外，参照 P0 既有约定）。
- **密钥永不进 Renderer**：P2 不新增任何读取 APIKey 明文的 IPC。
- **危险操作永不自动执行**：write_file / edit_file / run_shell 必须 `await approvals.request(...)`，用户未决策前一直挂起。
- **不做"永久允许"**：审批三选项仅 deny / allow once / allow session（本会话）。
- **zod 版本**：`zod@^3.25.0`（与 `ai@4.3.19` 的 peer 一致；lockfile 已有 `zod@3.25.76` 在依赖树中）。
- **AI SDK 字段名**：`streamText` 返回的 `fullStream` 中，tool-call 部分用 `part.args`（不是 `input`），tool-result 部分用 `part.result`（不是 `output`）。spec §6.2 的 `part.input`/`part.output` 写法与 `ai@4.3.19` 不符，本计划一律用正确字段名。
- **工作目录持久化**：跨重启保留，存于 SQLite `settings` 表（key-value），key 为 `'workspace'`。
- **ABI 冲突约定**（P0/P1 遗留环境债，SQLite 相关测试前需做）：跑测试前先 `cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3 && npm run install`（切到 Node ABI）；若之后要在 Electron 内运行，再 `npx node-gyp rebuild --target=30.5.1 --runtime=electron --disturl=https://electronjs.org/headers` 还原。本计划中 `corepack pnpm test` 即用 Node ABI。
- **包管理**：用 `corepack pnpm`（环境 PATH 无独立 pnpm）。
- **commit 信息**：中文描述 + 前缀（`feat(p2)`/`test(p2)`/`refactor(p2)`/`docs(p2)`）。
- **每个 task 末尾 commit + controller 统一 push**（不在 task 内 push）。

---

## 文件结构（P2 全景）

**新建（Main）：**
- `apps/desktop/src/main/tools/paths.ts` — 工作目录根 + 路径解析（相对→绝对）
- `apps/desktop/src/main/tools/infos.ts` — 静态 `TOOL_INFOS: ToolInfo[]`（供 IPC `tools:list`）
- `apps/desktop/src/main/tools/registry.ts` — `buildToolRegistry(approvals, sessionId)` 聚合 7 工具
- `apps/desktop/src/main/tools/builtins/readFile.ts` — read_file 工具
- `apps/desktop/src/main/tools/builtins/listDirectory.ts` — list_directory 工具
- `apps/desktop/src/main/tools/builtins/glob.ts` — glob 工具
- `apps/desktop/src/main/tools/builtins/grep.ts` — grep 工具
- `apps/desktop/src/main/tools/builtins/writeFile.ts` — write_file 工具（工厂）
- `apps/desktop/src/main/tools/builtins/editFile.ts` — edit_file 工具（工厂）
- `apps/desktop/src/main/tools/builtins/runShell.ts` — run_shell 工具（工厂）
- `apps/desktop/src/main/approval/queue.ts` — `ApprovalQueue` 类 + `getApprovalQueue()` 单例
- `apps/desktop/src/main/store/settings.ts` — key-value settings store（workspace 等）
- `apps/desktop/src/main/ipc/handlers/tools.ts` — tools/approval/workspace IPC handlers

**新建（Shared）：**
- `packages/shared/src/tool.ts` — 类型：`ToolName`/`ToolRisk`/`ToolInfo`/`ApprovalRequest`/`ApprovalDecision`/`ToolCallEvent`/`ToolResultEvent`

**新建（Renderer）：**
- `apps/desktop/src/renderer/components/ApprovalDialog.tsx` + `.module.css` — 朱砂印审批弹窗
- `apps/desktop/src/renderer/components/ToolCallView.tsx` + `.module.css` — 工具调用折叠卡片

**修改：**
- `packages/shared/src/ipc.ts` — 加 7 个 IPC 通道常量 + `ExposedApi.tools`
- `packages/shared/src/index.ts` — re-export `./tool.js`
- `apps/desktop/src/main/chat/run.ts` — streamText 加 `tools` + `maxSteps: 25`，`textStream`→`fullStream`
- `apps/desktop/src/main/store/migration.ts` — 加 version 4（settings 表）
- `apps/desktop/src/main/store/index.ts` — re-export `createSettingsStore`
- `apps/desktop/src/main/ipc/register.ts` — 注册 tools handlers
- `apps/desktop/src/main/index.ts` — 启动时初始化 workspaceRoot + 首次启动弹目录选择器
- `apps/desktop/src/preload/index.ts` — 暴露 `tools` 命名空间
- `apps/desktop/src/renderer/stores/chat.ts` — 加 toolCalls 状态 + 监听 onToolCall/onToolResult/onApprovalRequest
- `apps/desktop/src/renderer/pages/ChatPage.tsx` — 渲染 ToolCallView
- `apps/desktop/src/renderer/pages/SettingsPage.tsx` — 加"工作目录 · 案牍之所"区域
- `apps/desktop/src/renderer/ipc/client.ts` — 注释更新（tools 命名空间可用）
- `apps/desktop/tests/chat.test.ts` — mock fullStream
- `apps/desktop/package.json` — 加 zod 依赖

**新建测试：**
- `apps/desktop/tests/paths.test.ts`
- `apps/desktop/tests/tools.readonly.test.ts`
- `apps/desktop/tests/tools.dangerous.test.ts`
- `apps/desktop/tests/approval-queue.test.ts`
- `apps/desktop/tests/settings.test.ts`

---

## Task P2.0：shared 类型与 IPC 契约

**Files:**
- Create: `packages/shared/src/tool.ts`
- Modify: `packages/shared/src/ipc.ts`（加通道常量 + `ExposedApi.tools`）
- Modify: `packages/shared/src/index.ts`（re-export tool）

**Interfaces:**
- Produces: `ToolName`/`ToolRisk`/`ToolInfo`/`ApprovalRequest`/`ApprovalDecision`/`ToolCallEvent`/`ToolResultEvent`（类型，从 `@qiming/shared` 可见）；`IPC.CHAT_TOOL_CALL`/`CHAT_TOOL_RESULT`/`CHAT_APPROVAL_REQUEST`/`CHAT_APPROVAL_RESPOND`/`TOOLS_LIST`/`TOOLS_SET_WORKSPACE`/`TOOLS_GET_WORKSPACE`（常量）；`ExposedApi.tools`（接口形状）

- [ ] **Step 1: 写 `packages/shared/src/tool.ts`**

```typescript
// packages/shared/src/tool.ts
/** P2 工具系统类型契约。Main/Renderer/preload 三处共用。 */

export type ToolName =
  | 'read_file'
  | 'list_directory'
  | 'glob'
  | 'grep'
  | 'write_file'
  | 'edit_file'
  | 'run_shell';

export type ToolRisk = 'readonly' | 'dangerous';

export interface ToolInfo {
  name: ToolName;
  description: string;
  risk: ToolRisk;
}

/** Main→Renderer 推送的审批请求 */
export interface ApprovalRequest {
  id: string;
  sessionId: string;
  tool: ToolName;
  summary: string;
  detail: Record<string, unknown>;
  createdAt: number;
}

/** 审批决策：deny 拒绝 / allow 允许本次 / allow_session 本会话总允许（不持久） */
export type ApprovalDecision = 'deny' | 'allow' | 'allow_session';

/** 工具调用事件（M→R 推送，出现在消息流中） */
export interface ToolCallEvent {
  sessionId: string;
  /** 单次调用唯一 id，用于把后续 ToolResultEvent 配对到同一调用 */
  callId: string;
  tool: ToolName;
  args: Record<string, unknown>;
}

export interface ToolResultEvent {
  sessionId: string;
  callId: string;
  tool: ToolName;
  result: unknown;
  ok: boolean;
}
```

- [ ] **Step 2: 在 `packages/shared/src/ipc.ts` 加 IPC 通道常量**

在 `IPC` 对象内、`MEMORY_REEMBED` 之后、`WINDOW_*` 之前插入 P2 通道块：

```typescript
  // P2：工具系统 + 审批通道
  CHAT_TOOL_CALL: 'chat:tool_call',
  CHAT_TOOL_RESULT: 'chat:tool_result',
  CHAT_APPROVAL_REQUEST: 'chat:approval_request',
  CHAT_APPROVAL_RESPOND: 'chat:approval_respond',
  TOOLS_LIST: 'tools:list',
  TOOLS_SET_WORKSPACE: 'tools:set_workspace',
  TOOLS_GET_WORKSPACE: 'tools:get_workspace',
```

- [ ] **Step 3: 在 `ExposedApi` 接口加 `tools` 命名空间**

在 `memory: { ... };` 之后、接口闭合 `}` 之前追加：

```typescript
  /** P2：工具系统 + 审批。
   *  - list/getWorkspace/setWorkspace：工具元信息与工作目录管理。
   *  - onApprovalRequest：危险工具执行前 Main 推过来的审批请求；
   *    Renderer 必须用 respondApproval 回传决策，否则工具会一直挂起。
   *  - onToolCall/onToolResult：消息流中的工具调用可视化事件。 */
  tools: {
    list(): Promise<ToolInfo[]>;
    setWorkspace(path: string): Promise<void>;
    getWorkspace(): Promise<string>;
    onApprovalRequest(cb: (req: ApprovalRequest) => void): () => void;
    respondApproval(id: string, decision: ApprovalDecision): Promise<void>;
    onToolCall(cb: (p: ToolCallEvent) => void): () => void;
    onToolResult(cb: (p: ToolResultEvent) => void): () => void;
  };
```

并在 `ipc.ts` 顶部 import 块追加：

```typescript
import type {
  ToolInfo,
  ApprovalRequest,
  ApprovalDecision,
  ToolCallEvent,
  ToolResultEvent,
} from './tool.js';
```

- [ ] **Step 4: 在 `packages/shared/src/index.ts` re-export**

追加一行：

```typescript
export * from './tool.js';
```

- [ ] **Step 5: 类型检查（shared 是 type-only 包，无运行时测试，靠 tsc 验证）**

Run: `cd packages/shared && corepack pnpm exec tsc --noEmit`
Expected: 通过，无报错（若 shared 无独立 tsconfig，则 `cd apps/desktop && corepack pnpm exec tsc --noEmit -p tsconfig.node.json` 验证 main 侧能看到新类型；此项如环境不便可由 controller 在 P2.1 起统一 `corepack pnpm exec tsc --noEmit` 验证）。

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/tool.ts packages/shared/src/ipc.ts packages/shared/src/index.ts
git commit -m "feat(p2.0): shared 类型与 IPC 契约 - 工具/审批/工作目录通道"
```

---

## Task P2.1：工具基础设施（zod + paths + settings 持久化）

**Files:**
- Modify: `apps/desktop/package.json`（加 zod）
- Create: `apps/desktop/src/main/tools/paths.ts`
- Create: `apps/desktop/src/main/store/settings.ts`
- Modify: `apps/desktop/src/main/store/migration.ts`（version 4）
- Modify: `apps/desktop/src/main/store/index.ts`（re-export）
- Test: `apps/desktop/tests/paths.test.ts`
- Test: `apps/desktop/tests/settings.test.ts`

**Interfaces:**
- Produces: `setWorkspaceRoot(path: string): void`、`getWorkspaceRoot(): string`、`resolvePath(p: string): string`（来自 `tools/paths.ts`）；`createSettingsStore(db): { get(key): string | null; set(key, value): void; all(): Record<string,string> }`（来自 `store/settings.ts`）

- [ ] **Step 1: 装 zod**

Run:
```bash
cd apps/desktop && corepack pnpm add zod@^3.25.0
```
Expected: `zod` 写入 `apps/desktop/package.json` 的 dependencies，lockfile 更新。

- [ ] **Step 2: 写 `tools/paths.ts`**

```typescript
// apps/desktop/src/main/tools/paths.ts
import { resolve, isAbsolute } from 'node:path';

/**
 * 工作目录根。Main 进程模块级单例：
 *  - 应用启动时从 settings 表读取并 setWorkspaceRoot。
 *  - 用户在设置页改目录时，IPC handler 同时写 settings + setWorkspaceRoot。
 *
 * P2 不做路径白名单沙箱——审批机制是安全网。工作目录仅用于解析相对路径。
 */
let workspaceRoot = '';

export function setWorkspaceRoot(path: string): void {
  workspaceRoot = path;
}

export function getWorkspaceRoot(): string {
  return workspaceRoot;
}

/** 相对路径 → 相对 workspaceRoot 解析；绝对路径原样返回。 */
export function resolvePath(p: string): string {
  return isAbsolute(p) ? p : resolve(workspaceRoot, p);
}
```

- [ ] **Step 3: 写 `paths.test.ts`（先失败）**

```typescript
// apps/desktop/tests/paths.test.ts
import { describe, it, expect, afterEach } from 'vitest';
import { setWorkspaceRoot, getWorkspaceRoot, resolvePath } from '../src/main/tools/paths';

describe('tools/paths', () => {
  afterEach(() => setWorkspaceRoot(''));

  it('workspaceRoot 默认空串', () => {
    expect(getWorkspaceRoot()).toBe('');
  });

  it('setWorkspaceRoot 后可读', () => {
    setWorkspaceRoot('/tmp/proj');
    expect(getWorkspaceRoot()).toBe('/tmp/proj');
  });

  it('相对路径相对 workspaceRoot 解析', () => {
    setWorkspaceRoot('/tmp/proj');
    // 用 POSIX 风格断言（resolve 受 process.cwd 影响，故断言以 workspaceRoot 开头 + 含输入段）
    const r = resolvePath('src/a.ts');
    expect(r.startsWith('/tmp/proj')).toBe(true);
    expect(r.endsWith('src/a.ts')).toBe(true);
  });

  it('绝对路径原样返回', () => {
    setWorkspaceRoot('/tmp/proj');
    expect(resolvePath('/etc/hosts')).toBe('/etc/hosts');
  });
});
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/desktop && corepack pnpm test -- paths`
Expected: PASS 4。

- [ ] **Step 5: 写 `store/settings.ts`**

```typescript
// apps/desktop/src/main/store/settings.ts
import type { Database } from 'better-sqlite3';

/**
 * key-value settings store（P2.1 新增）。
 * 用途：工作目录等简单持久化配置。复杂结构用 JSON.stringify 存。
 */
export function createSettingsStore(db: Database) {
  return {
    get(key: string): string | null {
      const row = db
        .prepare('SELECT value FROM settings WHERE key = ?')
        .get(key) as { value: string } | undefined;
      return row?.value ?? null;
    },
    set(key: string, value: string): void {
      db.prepare(
        `INSERT INTO settings (key, value, updated_at) VALUES (?, ?, ?)
         ON CONFLICT(key) DO UPDATE SET value = excluded.value, updated_at = excluded.updated_at`,
      ).run(key, value, Date.now());
    },
    all(): Record<string, string> {
      const rows = db.prepare('SELECT key, value FROM settings').all() as {
        key: string;
        value: string;
      }[];
      const out: Record<string, string> = {};
      for (const r of rows) out[r.key] = r.value;
      return out;
    },
  };
}
```

- [ ] **Step 6: 在 migration.ts 加 version 4**

在 `migrations` 数组末尾（version 3 之后）追加：

```typescript
  {
    // P2.1: settings key-value 表（工作目录等持久化）。
    //  幂等：IF NOT EXISTS。
    version: 4,
    name: 'p2-settings',
    sql: `
      CREATE TABLE IF NOT EXISTS settings (
        key TEXT PRIMARY KEY,
        value TEXT NOT NULL,
        updated_at INTEGER NOT NULL
      );
    `,
  },
```

并在 migration.ts 顶部的当前版本号常量更新（若有 `CURRENT_VERSION`/`LATEST_VERSION` 常量则改为 4；若该常量是从 `migrations` 末尾推导则无需改）。**实施时先读 migration.ts 全文确认是否有显式版本号常量需同步**。

- [ ] **Step 7: 在 `store/index.ts` re-export**

追加：

```typescript
export { createSettingsStore } from './settings';
```

- [ ] **Step 8: 写 `settings.test.ts`（先失败）**

```typescript
// apps/desktop/tests/settings.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { runMigrations } from '../src/main/store/migration';
import { createSettingsStore } from '../src/main/store/settings';

function memDb(): Database.Database {
  const db = new Database(':memory:');
  db.pragma('foreign_keys = ON');
  runMigrations(db);
  return db;
}

describe('settingsStore', () => {
  let db: Database.Database;
  beforeEach(() => {
    db = memDb();
  });
  it('未设置时 get 返回 null', () => {
    const s = createSettingsStore(db);
    expect(s.get('nope')).toBeNull();
  });
  it('set 后 get 返回值', () => {
    const s = createSettingsStore(db);
    s.set('workspace', '/tmp/proj');
    expect(s.get('workspace')).toBe('/tmp/proj');
  });
  it('重复 set 同 key 覆盖（不报 UNIQUE 冲突）', () => {
    const s = createSettingsStore(db);
    s.set('workspace', '/a');
    s.set('workspace', '/b');
    expect(s.get('workspace')).toBe('/b');
  });
  it('all 返回全部 key-value', () => {
    const s = createSettingsStore(db);
    s.set('workspace', '/a');
    s.set('theme', 'gufeng');
    expect(s.all()).toEqual({ workspace: '/a', theme: 'gufeng' });
  });
  it('migration v4 后 settings 表存在', () => {
    // memDb 已 runMigrations；直接查表
    const tables = db
      .prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='settings'")
      .get();
    expect(tables).toBeDefined();
  });
});
```

- [ ] **Step 9: 跑测试（含 ABI 切换）**

Run:
```bash
cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3 && npm run install
```
（切到 Node ABI；若已切过可跳过）

然后：
```bash
cd apps/desktop && corepack pnpm test -- paths settings
```
Expected: paths 4 PASS + settings 5 PASS。

- [ ] **Step 10: 类型检查**

Run: `cd apps/desktop && corepack pnpm exec tsc --noEmit -p tsconfig.node.json`
Expected: 通过。

- [ ] **Step 11: Commit**

```bash
git add apps/desktop/package.json pnpm-lock.yaml apps/desktop/src/main/tools/paths.ts apps/desktop/src/main/store/settings.ts apps/desktop/src/main/store/migration.ts apps/desktop/src/main/store/index.ts apps/desktop/tests/paths.test.ts apps/desktop/tests/settings.test.ts
git commit -m "feat(p2.1): zod + paths 路径解析 + settings key-value 持久化（migration v4）"
```

---

## Task P2.2：4 个只读工具 + 注册表骨架

**Files:**
- Create: `apps/desktop/src/main/tools/builtins/readFile.ts`
- Create: `apps/desktop/src/main/tools/builtins/listDirectory.ts`
- Create: `apps/desktop/src/main/tools/builtins/glob.ts`
- Create: `apps/desktop/src/main/tools/builtins/grep.ts`
- Create: `apps/desktop/src/main/tools/infos.ts`
- Create: `apps/desktop/src/main/tools/registry.ts`
- Test: `apps/desktop/tests/tools.readonly.test.ts`

**Interfaces:**
- Consumes: `resolvePath` from `../paths`（P2.1）
- Produces: `readFileTool`/`listDirectoryTool`/`globTool`/`grepTool`（`Tool` 对象，含 `parameters` zod schema + `execute`）；`TOOL_INFOS: ToolInfo[]`（来自 `infos.ts`）；`buildToolRegistry(approvals, sessionId): Record<string, Tool>`（来自 `registry.ts`，本 task 只填只读 4 个，危险 3 个先留注释占位待 P2.4）

**约束（spec §4.3）：**
- read_file：读文本，>50000 字符截断（返回 `{ content, truncated }`）
- list_directory：返回 `{ name, type: 'file'|'dir' }[]`
- glob：模式搜文件名，上限 200 条
- grep：内容搜索，逐行扫，默认上限 50 结果
- 全部用 Node `fs/promises` + `readline`，**不依赖 ripgrep 等外部二进制**

- [ ] **Step 1: 写 `builtins/readFile.ts`**

```typescript
// apps/desktop/src/main/tools/builtins/readFile.ts
import { z } from 'zod';
import { readFile as fsReadFile } from 'node:fs/promises';
import type { Tool } from 'ai';
import { resolvePath } from '../paths';

const MAX_CHARS = 50000;

const parameters = z.object({
  path: z.string().describe('文件路径，绝对或相对工作目录'),
});

export const readFileTool: Tool<typeof parameters, { content: string; truncated: boolean }> = {
  description:
    '读取文本文件内容。超过 50000 字符会截断并在 truncated 字段标记。用于查看源码、配置、日志等。',
  parameters,
  async execute({ path }) {
    const full = resolvePath(path);
    const buf = await fsReadFile(full, 'utf-8');
    if (buf.length > MAX_CHARS) {
      return { content: buf.slice(0, MAX_CHARS), truncated: true };
    }
    return { content: buf, truncated: false };
  },
};
```

- [ ] **Step 2: 写 `builtins/listDirectory.ts`**

```typescript
// apps/desktop/src/main/tools/builtins/listDirectory.ts
import { z } from 'zod';
import { readdir, stat } from 'node:fs/promises';
import { resolvePath } from '../paths';
import type { Tool } from 'ai';

const parameters = z.object({
  path: z.string().describe('目录路径，绝对或相对工作目录'),
});

interface Entry {
  name: string;
  type: 'file' | 'dir';
}

export const listDirectoryTool: Tool<typeof parameters, Entry[]> = {
  description: '列出目录下的文件与子目录（不递归）。每项返回 name 与 type。',
  parameters,
  async execute({ path }) {
    const full = resolvePath(path);
    const names = await readdir(full);
    const out: Entry[] = [];
    for (const name of names) {
      let isDir = false;
      try {
        isDir = (await stat(`${full}/${name}`)).isDirectory();
      } catch {
        isDir = false;
      }
      out.push({ name, type: isDir ? 'dir' : 'file' });
    }
    return out;
  },
};
```

- [ ] **Step 3: 写 `builtins/glob.ts`**

简单实现：递归扫工作目录，按 `*`/`**` 通配匹配文件相对路径。不引第三方 glob 库（YAGNI），用最小手写匹配（支持 `**` 跨层、`*` 单层通配、`?` 单字符）。

```typescript
// apps/desktop/src/main/tools/builtins/glob.ts
import { z } from 'zod';
import { readdir, stat } from 'node:fs/promises';
import { relative, resolve, join } from 'node:path';
import { resolvePath } from '../paths';
import type { Tool } from 'ai';

const MAX_RESULTS = 200;

const parameters = z.object({
  pattern: z.string().describe('glob 模式，如 "**/*.ts" 或 "src/*.json"'),
  path: z
    .string()
    .optional()
    .describe('搜索根目录，默认工作目录'),
});

/** 把 glob 模式转成正则。支持 **（跨层）、*（不含分隔符）、?（单字符）。 */
function globToRe(pattern: string): RegExp {
  let re = '^';
  for (let i = 0; i < pattern.length; i++) {
    const c = pattern[i];
    if (c === '*') {
      if (pattern[i + 1] === '*') {
        // ** 跨任意层（含分隔符）
        re += '.*';
        i++;
        // 吃掉多余的 /
        if (pattern[i + 1] === '/') i++;
      } else {
        re += '[^/]*';
      }
    } else if (c === '?') {
      re += '[^/]';
    } else if ('.+()|^$\\{}[]'.includes(c)) {
      re += '\\' + c;
    } else {
      re += c;
    }
  }
  re += '$';
  return new RegExp(re);
}

export const globTool: Tool<
  typeof parameters,
  { matches: string[]; truncated: boolean }
> = {
  description:
    '按 glob 模式搜索文件路径（递归）。返回相对工作目录的匹配路径，上限 200 条。',
  parameters,
  async execute({ pattern, path }) {
    const root = path ? resolvePath(path) : resolvePath('.') === '.' ? '' : resolvePath('.');
    // 上面三元处理 '.' 在空 workspaceRoot 下；正常情况直接用 resolvePath(path ?? '.')
    const searchRoot = path ? resolvePath(path) : (resolvePath('.') || process.cwd());
    const re = globToRe(pattern);
    const matches: string[] = [];

    async function walk(dir: string) {
      if (matches.length >= MAX_RESULTS) return;
      let entries: string[] = [];
      try {
        entries = await readdir(dir);
      } catch {
        return;
      }
      for (const name of entries) {
        const abs = join(dir, name);
        let isDir = false;
        try {
          isDir = (await stat(abs)).isDirectory();
        } catch {
          isDir = false;
        }
        const rel = relative(searchRoot, abs).replace(/\\/g, '/');
        if (!isDir && re.test(rel)) {
          matches.push(rel);
          if (matches.length >= MAX_RESULTS) return;
        }
        if (isDir) await walk(abs);
      }
    }

    await walk(searchRoot);
    // 忽略 root 变量（仅为可读性保留），搜索以 searchRoot 为准
    void root;
    return { matches, truncated: matches.length >= MAX_RESULTS };
  },
};
```

> **注**：glob 实现保留 `root` 变量是为后续若需扩展预留，`void root` 避免未用告警。reviewer 若认为多余可精简，但功能须等价。

- [ ] **Step 4: 写 `builtins/grep.ts`**

```typescript
// apps/desktop/src/main/tools/builtins/grep.ts
import { z } from 'zod';
import { readdir, stat } from 'node:fs/promises';
import { readFile } from 'node:fs/promises';
import { join, relative } from 'node:path';
import { resolvePath } from '../paths';
import type { Tool } from 'ai';

const DEFAULT_MAX = 50;

const parameters = z.object({
  pattern: z.string().describe('正则表达式（JS 语法）'),
  path: z.string().optional().describe('搜索根目录，默认工作目录'),
  glob: z
    .string()
    .optional()
    .describe('限定文件名 glob（简单子串匹配，非完整 glob 语法）'),
  maxResults: z
    .number()
    .int()
    .positive()
    .optional()
    .describe(`结果上限，默认 ${DEFAULT_MAX}`),
});

interface GrepHit {
  file: string;
  line: number;
  text: string;
}

export const grepTool: Tool<
  typeof parameters,
  { hits: GrepHit[]; truncated: boolean }
> = {
  description:
    '在文件内容中搜索正则。逐文件逐行扫描，返回 {file, line, text}。默认上限 50 条。',
  parameters,
  async execute({ pattern, path, glob, maxResults }) {
    const limit = maxResults ?? DEFAULT_MAX;
    const root = path ? resolvePath(path) : resolvePath('.') || process.cwd();
    const re = new RegExp(pattern);
    const hits: GrepHit[] = [];
    const globSub = glob ?? '';

    async function walk(dir: string) {
      if (hits.length >= limit) return;
      let entries: string[] = [];
      try {
        entries = await readdir(dir);
      } catch {
        return;
      }
      for (const name of entries) {
        if (hits.length >= limit) return;
        const abs = join(dir, name);
        let isDir = false;
        try {
          isDir = (await stat(abs)).isDirectory();
        } catch {
          continue;
        }
        if (isDir) {
          await walk(abs);
          continue;
        }
        if (globSub && !name.includes(globSub)) continue;
        let content: string;
        try {
          content = await readFile(abs, 'utf-8');
        } catch {
          continue;
        }
        const lines = content.split('\n');
        for (let i = 0; i < lines.length; i++) {
          if (hits.length >= limit) break;
          if (re.test(lines[i])) {
            hits.push({
              file: relative(root, abs).replace(/\\/g, '/'),
              line: i + 1,
              text: lines[i].length > 500 ? lines[i].slice(0, 500) + '…' : lines[i],
            });
          }
        }
      }
    }

    await walk(root);
    return { hits, truncated: hits.length >= limit };
  },
}
```

- [ ] **Step 5: 写 `tools/infos.ts`（静态元信息）**

```typescript
// apps/desktop/src/main/tools/infos.ts
import type { ToolInfo } from '@qiming/shared';

/** 供 IPC tools:list 返回。description 与各工具的 Tool.description 保持口径一致。 */
export const TOOL_INFOS: ToolInfo[] = [
  { name: 'read_file', description: '读取文本文件（>50000 字符截断）', risk: 'readonly' },
  { name: 'list_directory', description: '列出目录文件与子目录', risk: 'readonly' },
  { name: 'glob', description: '按 glob 模式搜索文件路径（上限 200）', risk: 'readonly' },
  { name: 'grep', description: '文件内容正则搜索（默认上限 50）', risk: 'readonly' },
  { name: 'write_file', description: '写入文件（覆盖）', risk: 'dangerous' },
  { name: 'edit_file', description: '精确替换文件内容', risk: 'dangerous' },
  { name: 'run_shell', description: '执行 shell 命令（30s 超时）', risk: 'dangerous' },
];
```

- [ ] **Step 6: 写 `tools/registry.ts`（只填只读 4 个）**

```typescript
// apps/desktop/src/main/tools/registry.ts
import type { Tool } from 'ai';
import type { ApprovalQueue } from '../approval/queue';
import { readFileTool } from './builtins/readFile';
import { listDirectoryTool } from './builtins/listDirectory';
import { globTool } from './builtins/glob';
import { grepTool } from './builtins/grep';

/**
 * 构建本会话的工具集。
 *  - 只读工具：静态对象，直接注入。
 *  - 危险工具：工厂函数（writeFile/editFile/runShell），注入 approvals + sessionId
 *    以便 execute 内部 await 审批。P2.4 之后补齐。
 */
export function buildToolRegistry(
  _approvals: ApprovalQueue,
  _sessionId: string,
): Record<string, Tool> {
  return {
    read_file: readFileTool,
    list_directory: listDirectoryTool,
    glob: globTool,
    grep: grepTool,
    // P2.4 追加：
    // write_file: writeFileTool(approvals, sessionId),
    // edit_file: editFileTool(approvals, sessionId),
    // run_shell: runShellTool(approvals, sessionId),
  };
}
```

> **注**：参数名前缀 `_` 表示本 task 暂未使用（避免 tsc `noUnusedParameters` 报错）。P2.4 启用危险工具时去掉下划线。`import type { ApprovalQueue }` 是类型导入，需 P2.3 先建 `approval/queue.ts`——若 P2.2 先于 P2.3 跑（依赖顺序允许），则此处改用一个本地最小占位类型 `type ApprovalQueue = { request(s: string, r: ApprovalRequest): Promise<ApprovalDecision> }`，待 P2.3 合并后切回真实导入。**实施时按依赖顺序确认**：本计划安排 P2.2 在 P2.3 之前，故本 task 用占位类型；P2.3 完成后 controller 在 P2.4 task 里把占位替换为真实导入（一并写进 P2.4 的 Files 清单）。为避免循环，本 task 先内联占位：

```typescript
// 占位（P2.3 之后替换为真实 ApprovalQueue）
type ApprovalQueue = unknown;
```

- [ ] **Step 7: 写 `tools.readonly.test.ts`（先失败）**

```typescript
// apps/desktop/tests/tools.readonly.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtemp, mkdir, writeFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { setWorkspaceRoot } from '../src/main/tools/paths';
import { readFileTool } from '../src/main/tools/builtins/readFile';
import { listDirectoryTool } from '../src/main/tools/builtins/listDirectory';
import { globTool } from '../src/main/tools/builtins/glob';
import { grepTool } from '../src/main/tools/builtins/grep';

let root: string;
beforeEach(async () => {
  root = await mkdtemp(join(tmpdir(), 'qiming-p2-'));
  setWorkspaceRoot(root);
  // 准备测试树
  await mkdir(join(root, 'src'), { recursive: true });
  await writeFile(join(root, 'a.txt'), 'hello world\nsecond line');
  await writeFile(join(root, 'src/b.ts'), 'export const x = 1;');
  await writeFile(join(root, 'src/c.ts'), 'export const y = 2;');
});
afterEach(async () => {
  setWorkspaceRoot('');
  await rm(root, { recursive: true, force: true });
});

describe('read_file', () => {
  it('读相对路径文件', async () => {
    const r = await readFileTool.execute({ path: 'a.txt' }, {} as never);
    expect(r.content).toContain('hello world');
    expect(r.truncated).toBe(false);
  });
  it('大文件截断', async () => {
    const big = 'x'.repeat(60000);
    await writeFile(join(root, 'big.txt'), big);
    const r = await readFileTool.execute({ path: 'big.txt' }, {} as never);
    expect(r.content.length).toBe(50000);
    expect(r.truncated).toBe(true);
  });
});

describe('list_directory', () => {
  it('列出文件与目录', async () => {
    const entries = await listDirectoryTool.execute({ path: '.' }, {} as never);
    const names = entries.map((e) => e.name).sort();
    expect(names).toContain('a.txt');
    expect(names).toContain('src');
    const src = entries.find((e) => e.name === 'src');
    expect(src?.type).toBe('dir');
  });
});

describe('glob', () => {
  it('**/*.ts 匹配 src 下 ts', async () => {
    const r = await globTool.execute({ pattern: '**/*.ts' }, {} as never);
    expect(r.matches.sort()).toEqual(['src/b.ts', 'src/c.ts']);
  });
  it('上限 200 截断标记', async () => {
    for (let i = 0; i < 210; i++) {
      await writeFile(join(root, `f${i}.dat`), 'x');
    }
    const r = await globTool.execute({ pattern: 'f*.dat' }, {} as never);
    expect(r.matches.length).toBe(200);
    expect(r.truncated).toBe(true);
  });
});

describe('grep', () => {
  it('按正则命中行', async () => {
    const r = await grepTool.execute({ pattern: 'export const' }, {} as never);
    expect(r.hits.length).toBe(2);
    expect(r.hits.every((h) => h.file.endsWith('.ts'))).toBe(true);
  });
  it('glob 过滤文件名', async () => {
    const r = await grepTool.execute({ pattern: 'x', glob: 'b.ts' }, {} as never);
    expect(r.hits.length).toBe(1);
    expect(r.hits[0].file).toBe('src/b.ts');
  });
});
```

- [ ] **Step 8: 跑测试**

Run: `cd apps/desktop && corepack pnpm test -- tools.readonly`
Expected: PASS（read_file 2 + list_directory 1 + glob 2 + grep 2 = 7）。

- [ ] **Step 9: 类型检查**

Run: `cd apps/desktop && corepack pnpm exec tsc --noEmit -p tsconfig.node.json`
Expected: 通过。

- [ ] **Step 10: Commit**

```bash
git add apps/desktop/src/main/tools/ apps/desktop/tests/tools.readonly.test.ts
git commit -m "feat(p2.2): 4 只读工具（read/list/glob/grep）+ 注册表骨架 + TOOL_INFOS"
```

---

## Task P2.3：审批引擎 + tools/workspace IPC handlers + preload 暴露

**Files:**
- Create: `apps/desktop/src/main/approval/queue.ts`
- Create: `apps/desktop/src/main/ipc/handlers/tools.ts`
- Modify: `apps/desktop/src/main/ipc/register.ts`
- Modify: `apps/desktop/src/preload/index.ts`（加 tools 命名空间）
- Test: `apps/desktop/tests/approval-queue.test.ts`
- Modify: `apps/desktop/src/main/tools/registry.ts`（占位 ApprovalQueue → 真实类型导入）

**Interfaces:**
- Consumes: `ApprovalRequest`/`ApprovalDecision` from `@qiming/shared`；`IPC.*` 通道；`createSettingsStore`/`getDb` from store；`setWorkspaceRoot`/`getWorkspaceRoot` from paths
- Produces: `ApprovalQueue` 类（`request(sessionId, req): Promise<ApprovalDecision>`、`respond(id, decision, sessionId, tool): void`、`clearSession(sessionId): void`）；`getApprovalQueue(): ApprovalQueue` 单例；`registerToolHandlers()` 注册函数

- [ ] **Step 1: 写 `approval/queue.ts`**

```typescript
// apps/desktop/src/main/approval/queue.ts
import { BrowserWindow } from 'electron';
import { IPC, type ApprovalRequest, type ApprovalDecision, type ToolName } from '@qiming/shared';

interface Pending {
  resolve: (d: ApprovalDecision) => void;
}

/**
 * 审批队列（Main 进程单例）。
 *
 * - pending：id → resolve 回调（每个请求挂起一个 Promise，等 Renderer 决策）。
 * - sessionAllowed：sessionId → 本会话总允许的工具集合。
 *   切换会话/重启 → Map 是内存态自动重置。
 *
 * request() 先查 sessionAllowed，命中则直接 'allow'（不弹窗）；
 * 否则经 IPC 推 CHAT_APPROVAL_REQUEST 给 Renderer，挂起 Promise。
 */
export class ApprovalQueue {
  private pending = new Map<string, Pending>();
  private sessionAllowed = new Map<string, Set<ToolName>>();

  async request(sessionId: string, req: ApprovalRequest): Promise<ApprovalDecision> {
    if (this.sessionAllowed.get(sessionId)?.has(req.tool)) return 'allow';
    const win = BrowserWindow.getAllWindows()[0];
    win?.webContents.send(IPC.CHAT_APPROVAL_REQUEST, req);
    return new Promise<ApprovalDecision>((resolve) => {
      this.pending.set(req.id, { resolve });
    });
  }

  respond(
    id: string,
    decision: ApprovalDecision,
    sessionId: string,
    tool: ToolName,
  ): void {
    const p = this.pending.get(id);
    if (p) {
      p.resolve(decision);
      this.pending.delete(id);
    }
    if (decision === 'allow_session') {
      if (!this.sessionAllowed.has(sessionId)) {
        this.sessionAllowed.set(sessionId, new Set());
      }
      this.sessionAllowed.get(sessionId)!.add(tool);
    }
  }

  /** 清空某会话的总允许集合（测试辅助；切换会话不主动调，靠内存态自然重置）。 */
  clearSession(sessionId: string): void {
    this.sessionAllowed.delete(sessionId);
  }

  /** 测试辅助：pending 是否为空。 */
  hasNoPending(): boolean {
    return this.pending.size === 0;
  }
}

let singleton: ApprovalQueue | null = null;
export function getApprovalQueue(): ApprovalQueue {
  if (!singleton) singleton = new ApprovalQueue();
  return singleton;
}
```

- [ ] **Step 2: 写 `approval-queue.test.ts`（先失败）**

```typescript
// apps/desktop/tests/approval-queue.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';

// mock electron BrowserWindow.getAllWindows（queue.request 会调它）
vi.mock('electron', () => ({
  BrowserWindow: {
    getAllWindows: vi.fn(() => []),
  },
}));

import { ApprovalQueue } from '../src/main/approval/queue';
import type { ApprovalRequest, ToolName } from '@qiming/shared';

function mkReq(id: string, tool: ToolName = 'write_file'): ApprovalRequest {
  return {
    id,
    sessionId: 's1',
    tool,
    summary: 'test',
    detail: {},
    createdAt: 0,
  };
}

describe('ApprovalQueue', () => {
  let q: ApprovalQueue;
  beforeEach(() => {
    q = new ApprovalQueue();
  });

  it('request 返回的 Promise 在 respond 后 resolve', async () => {
    const p = q.request('s1', mkReq('r1'));
    expect(q.hasNoPending()).toBe(false);
    q.respond('r1', 'allow', 's1', 'write_file');
    await expect(p).resolves.toBe('allow');
    expect(q.hasNoPending()).toBe(true);
  });

  it('respond allow 后 sessionAllowed 不变（allow 只本次）', async () => {
    const p1 = q.request('s1', mkReq('r1'));
    q.respond('r1', 'allow', 's1', 'write_file');
    await p1;
    // 再次 request 同会话同工具 → 不应命中缓存，仍挂起
    const p2 = q.request('s1', mkReq('r2'));
    expect(q.hasNoPending()).toBe(true);
    q.respond('r2', 'deny', 's1', 'write_file');
    await p2;
  });

  it('respond allow_session 后同会话同工具直接放行', async () => {
    const p1 = q.request('s1', mkReq('r1', 'write_file'));
    q.respond('r1', 'allow_session', 's1', 'write_file');
    await p1;
    // 再次 request → 立即 allow，不挂起
    const p2 = q.request('s1', mkReq('r2', 'write_file'));
    await expect(p2).resolves.toBe('allow');
    expect(q.hasNoPending()).toBe(true);
  });

  it('allow_session 按 sessionId 隔离', async () => {
    const p1 = q.request('s1', mkReq('r1', 'write_file'));
    q.respond('r1', 'allow_session', 's1', 'write_file');
    await p1;
    // 另一会话同工具 → 仍挂起
    const p2 = q.request('s2', mkReq('r2', 'write_file'));
    expect(q.hasNoPending()).toBe(false);
    q.respond('r2', 'deny', 's2', 'write_file');
    await p2;
  });

  it('respond 未知 id 不抛错（幂等）', () => {
    expect(() => q.respond('unknown', 'allow', 's1', 'write_file')).not.toThrow();
  });

  it('deny 决策 resolve 为 deny', async () => {
    const p = q.request('s1', mkReq('r1'));
    q.respond('r1', 'deny', 's1', 'write_file');
    await expect(p).resolves.toBe('deny');
  });

  it('clearSession 后总允许失效', async () => {
    const p1 = q.request('s1', mkReq('r1', 'write_file'));
    q.respond('r1', 'allow_session', 's1', 'write_file');
    await p1;
    q.clearSession('s1');
    const p2 = q.request('s1', mkReq('r2', 'write_file'));
    expect(q.hasNoPending()).toBe(false);
    q.respond('r2', 'deny', 's1', 'write_file');
    await p2;
  });
});
```

- [ ] **Step 3: 跑测试**

Run: `cd apps/desktop && corepack pnpm test -- approval-queue`
Expected: PASS 7。

- [ ] **Step 4: 写 `ipc/handlers/tools.ts`**

```typescript
// apps/desktop/src/main/ipc/handlers/tools.ts
import { ipcMain } from 'electron';
import { IPC } from '@qiming/shared';
import { getDb, createSettingsStore } from '../../store';
import { TOOL_INFOS } from '../../tools/infos';
import { setWorkspaceRoot, getWorkspaceRoot } from '../../tools/paths';
import { getApprovalQueue } from '../../approval/queue';

const WORKSPACE_KEY = 'workspace';

/** tools/approval/workspace IPC handlers。 */
export function registerToolHandlers() {
  // 启动时把 settings 里的 workspace 灌进 paths 模块（在本函数被调时执行一次）
  const db = getDb();
  const settings = createSettingsStore(db);
  const saved = settings.get(WORKSPACE_KEY);
  if (saved) setWorkspaceRoot(saved);

  ipcMain.handle(IPC.TOOLS_LIST, () => TOOL_INFOS);

  ipcMain.handle(IPC.TOOLS_GET_WORKSPACE, () => getWorkspaceRoot());

  ipcMain.handle(IPC.TOOLS_SET_WORKSPACE, (_e, path: string) => {
    setWorkspaceRoot(path);
    settings.set(WORKSPACE_KEY, path);
  });

  ipcMain.handle(
    IPC.CHAT_APPROVAL_RESPOND,
    (_e, id: string, decision: 'allow' | 'deny' | 'allow_session', sessionId: string, tool: ToolName) => {
      getApprovalQueue().respond(id, decision, sessionId, tool);
    },
  );
}
```

> **import 修正**：`CHAT_APPROVAL_RESPOND` handler 需要 `ToolName` 类型，在文件顶部 import 块补 `import type { ToolName } from '@qiming/shared';`。

- [ ] **Step 5: 在 `register.ts` 注册**

修改 `apps/desktop/src/main/ipc/register.ts`，加 import 与调用：

```typescript
import { registerToolHandlers } from './handlers/tools';

export function registerIpc() {
  registerProviderHandlers();
  registerSessionHandlers();
  registerChatHandlers();
  registerWindowHandlers();
  registerMemoryHandlers();
  registerToolHandlers();
}
```

- [ ] **Step 6: 在 `preload/index.ts` 加 tools 命名空间**

在 `api` 对象的 `memory: { ... }` 之后追加：

```typescript
  tools: {
    list: () => ipcRenderer.invoke(IPC.TOOLS_LIST),
    setWorkspace: (path) => ipcRenderer.invoke(IPC.TOOLS_SET_WORKSPACE, path),
    getWorkspace: () => ipcRenderer.invoke(IPC.TOOLS_GET_WORKSPACE),
    respondApproval: (id, decision) =>
      ipcRenderer.invoke(IPC.CHAT_APPROVAL_RESPOND, id, decision),
    onApprovalRequest: (cb) => {
      const h = (_e: IpcRendererEvent, p: Parameters<typeof cb>[0]) => cb(p);
      ipcRenderer.on(IPC.CHAT_APPROVAL_REQUEST, h);
      return () => {
        ipcRenderer.off(IPC.CHAT_APPROVAL_REQUEST, h);
      };
    },
    onToolCall: (cb) => {
      const h = (_e: IpcRendererEvent, p: Parameters<typeof cb>[0]) => cb(p);
      ipcRenderer.on(IPC.CHAT_TOOL_CALL, h);
      return () => {
        ipcRenderer.off(IPC.CHAT_TOOL_CALL, h);
      };
    },
    onToolResult: (cb) => {
      const h = (_e: IpcRendererEvent, p: Parameters<typeof cb>[0]) => cb(p);
      ipcRenderer.on(IPC.CHAT_TOOL_RESULT, h);
      return () => {
        ipcRenderer.off(IPC.CHAT_TOOL_RESULT, h);
      };
    },
  },
```

> **注**：`respondApproval(id, decision)` 只传 2 个参数；Main 侧 handler 需要 4 个（id/decision/sessionId/tool）。sessionId 与 tool 由 Main 侧的 `ApprovalQueue` 在 `request` 时已知——但 `respond` 签名需要它们。**修正方案**：把 sessionId/tool 放进 ApprovalRequest 时就由工具传入（工具 execute 时已知 sessionId 与 tool name），Renderer 回传 decision 时连同原 req 一起回——即 preload 签名改为 `respondApproval(req: ApprovalRequest, decision)`。更新 preload：

```typescript
    respondApproval: (req, decision) =>
      ipcRenderer.invoke(IPC.CHAT_APPROVAL_RESPOND, req, decision),
```

并同步更新 P2.0 中 `ExposedApi.tools.respondApproval` 签名为 `(req: ApprovalRequest, decision: ApprovalDecision): Promise<void>`（**这是 P2.0 的一处修正，controller 在执行 P2.3 前先回 P2.0 文件改此签名，或在 P2.3 内一并改 shared/ipc.ts**——本计划默认 P2.3 内同步修 shared，commit 信息注明）。

handler 改为：

```typescript
  ipcMain.handle(
    IPC.CHAT_APPROVAL_RESPOND,
    (_e, req: ApprovalRequest, decision: ApprovalDecision) => {
      getApprovalQueue().respond(req.id, decision, req.sessionId, req.tool);
    },
  );
```

顶部 import 改为：`import type { ApprovalRequest, ApprovalDecision } from '@qiming/shared';`

- [ ] **Step 7: 更新 registry.ts 占位 → 真实导入**

把 P2.2 的 `type ApprovalQueue = unknown;` 替换为：

```typescript
import type { ApprovalQueue } from '../approval/queue';
```

并把 `buildToolRegistry` 参数 `_approvals`/`_sessionId` 去掉下划线（P2.4 会用到，先保留名）。

- [ ] **Step 8: 类型检查**

Run: `cd apps/desktop && corepack pnpm exec tsc --noEmit -p tsconfig.node.json`
Expected: 通过。

- [ ] **Step 9: Commit**

```bash
git add apps/desktop/src/main/approval/queue.ts apps/desktop/src/main/ipc/handlers/tools.ts apps/desktop/src/main/ipc/register.ts apps/desktop/src/main/tools/registry.ts apps/desktop/src/preload/index.ts apps/desktop/tests/approval-queue.test.ts packages/shared/src/ipc.ts
git commit -m "feat(p2.3): 审批引擎 ApprovalQueue + tools/workspace IPC + preload tools 命名空间"
```

---

## Task P2.4：3 个危险工具（write_file / edit_file / run_shell）

**Files:**
- Create: `apps/desktop/src/main/tools/builtins/writeFile.ts`
- Create: `apps/desktop/src/main/tools/builtins/editFile.ts`
- Create: `apps/desktop/src/main/tools/builtins/runShell.ts`
- Modify: `apps/desktop/src/main/tools/registry.ts`（启用 3 个危险工具）
- Test: `apps/desktop/tests/tools.dangerous.test.ts`

**Interfaces:**
- Consumes: `ApprovalQueue.request(sessionId, req)` from `../../approval/queue`；`resolvePath` from `../paths`
- Produces: `writeFileTool(approvals, sessionId): Tool`、`editFileTool(approvals, sessionId): Tool`、`runShellTool(approvals, sessionId): Tool`（工厂）

**约束（spec §4.4）：**
- write_file：摘要 `"写入文件：{path}"`，detail 含 `contentPreview`（前 500 字符）
- edit_file：`oldText` 必须精确匹配；摘要 `"编辑文件：{path}（替换 N 处）"`
- run_shell：命令明文；30s 超时；stdout/stderr 各截断 10000 字符
- deny → 返回 `{ ok: false, error: '用户拒绝' }`

- [ ] **Step 1: 写 `builtins/writeFile.ts`**

```typescript
// apps/desktop/src/main/tools/builtins/writeFile.ts
import { z } from 'zod';
import { writeFile as fsWriteFile, mkdir } from 'node:fs/promises';
import { dirname } from 'node:path';
import { randomUUID } from 'node:crypto';
import type { Tool } from 'ai';
import type { ApprovalQueue } from '../../approval/queue';
import { resolvePath } from '../paths';

const PREVIEW_LEN = 500;

export function writeFileTool(
  approvals: ApprovalQueue,
  sessionId: string,
): Tool {
  const parameters = z.object({
    path: z.string().describe('文件路径，绝对或相对工作目录'),
    content: z.string().describe('要写入的完整内容（覆盖现有）'),
  });
  return {
    description: '写入文件（覆盖）。需用户审批。自动创建父目录。',
    parameters,
    async execute({ path, content }) {
      const full = resolvePath(path);
      const decision = await approvals.request(sessionId, {
        id: randomUUID(),
        sessionId,
        tool: 'write_file',
        summary: `写入文件：${path}`,
        detail: { path: full, contentPreview: content.slice(0, PREVIEW_LEN) },
        createdAt: Date.now(),
      });
      if (decision === 'deny') return { ok: false, error: '用户拒绝' };
      await mkdir(dirname(full), { recursive: true });
      await fsWriteFile(full, content, 'utf-8');
      return { ok: true, path: full, bytes: content.length };
    },
  };
}
```

- [ ] **Step 2: 写 `builtins/editFile.ts`**

```typescript
// apps/desktop/src/main/tools/builtins/editFile.ts
import { z } from 'zod';
import { readFile, writeFile } from 'node:fs/promises';
import { randomUUID } from 'node:crypto';
import type { Tool } from 'ai';
import type { ApprovalQueue } from '../../approval/queue';
import { resolvePath } from '../paths';

export function editFileTool(
  approvals: ApprovalQueue,
  sessionId: string,
): Tool {
  const parameters = z.object({
    path: z.string().describe('文件路径，绝对或相对工作目录'),
    oldText: z.string().describe('要被替换的精确文本（必须唯一匹配）'),
    newText: z.string().describe('替换为的文本'),
  });
  return {
    description:
      '编辑文件：把所有精确匹配 oldText 处替换为 newText。需用户审批。',
    parameters,
    async execute({ path, oldText, newText }) {
      const full = resolvePath(path);
      const original = await readFile(full, 'utf-8');
      // 统计匹配数
      let count = 0;
      let idx = 0;
      while ((idx = original.indexOf(oldText, idx)) !== -1) {
        count++;
        idx += oldText.length;
      }
      if (count === 0) {
        return { ok: false, error: 'oldText 在文件中未匹配' };
      }
      const decision = await approvals.request(sessionId, {
        id: randomUUID(),
        sessionId,
        tool: 'edit_file',
        summary: `编辑文件：${path}（替换 ${count} 处）`,
        detail: {
          path: full,
          oldTextPreview: oldText.slice(0, 300),
          newTextPreview: newText.slice(0, 300),
          count,
        },
        createdAt: Date.now(),
      });
      if (decision === 'deny') return { ok: false, error: '用户拒绝' };
      const replaced = original.split(oldText).join(newText);
      await writeFile(full, replaced, 'utf-8');
      return { ok: true, path: full, replaced: count };
    },
  };
}
```

- [ ] **Step 3: 写 `builtins/runShell.ts`**

```typescript
// apps/desktop/src/main/tools/builtins/runShell.ts
import { z } from 'zod';
import { spawn } from 'node:child_process';
import { randomUUID } from 'node:crypto';
import type { Tool } from 'ai';
import type { ApprovalQueue } from '../../approval/queue';
import { resolvePath } from '../paths';

const TIMEOUT_MS = 30_000;
const MAX_OUTPUT = 10_000;

export function runShellTool(
  approvals: ApprovalQueue,
  sessionId: string,
): Tool {
  const parameters = z.object({
    command: z.string().describe('要执行的 shell 命令（明文，单行）'),
    cwd: z.string().optional().describe('工作目录，默认工作目录根'),
  });
  return {
    description:
      '执行 shell 命令。30 秒超时。stdout/stderr 各截断 10000 字符。需用户审批。',
    parameters,
    async execute({ command, cwd }) {
      const workDir = cwd ? resolvePath(cwd) : resolvePath('.') || process.cwd();
      const decision = await approvals.request(sessionId, {
        id: randomUUID(),
        sessionId,
        tool: 'run_shell',
        summary: `执行命令：${command}`,
        detail: { command, cwd: workDir },
        createdAt: Date.now(),
      });
      if (decision === 'deny') return { ok: false, error: '用户拒绝' };

      return new Promise((resolve) => {
        let stdout = '';
        let stderr = '';
        let timer: NodeJS.Timeout | null = null;
        const child = spawn(command, {
          cwd: workDir,
          shell: true,
          windowsHide: true,
        });
        child.stdout.on('data', (d: Buffer) => {
          stdout += d.toString();
          if (stdout.length > MAX_OUTPUT) stdout = stdout.slice(0, MAX_OUTPUT);
        });
        child.stderr.on('data', (d: Buffer) => {
          stderr += d.toString();
          if (stderr.length > MAX_OUTPUT) stderr = stderr.slice(0, MAX_OUTPUT);
        });
        child.on('error', (err) => {
          if (timer) clearTimeout(timer);
          resolve({ ok: false, error: err.message, stdout, stderr });
        });
        child.on('close', (code) => {
          if (timer) clearTimeout(timer);
          resolve({
            ok: code === 0,
            code,
            stdout: stdout.slice(0, MAX_OUTPUT),
            stderr: stderr.slice(0, MAX_OUTPUT),
          });
        });
        timer = setTimeout(() => {
          child.kill('SIGTERM');
          resolve({ ok: false, error: '超时（30s）', stdout, stderr });
        }, TIMEOUT_MS);
      });
    },
  };
}
```

- [ ] **Step 4: 更新 `registry.ts` 启用危险工具**

把注释占位替换为真实导入与启用：

```typescript
// apps/desktop/src/main/tools/registry.ts
import type { Tool } from 'ai';
import type { ApprovalQueue } from '../approval/queue';
import { readFileTool } from './builtins/readFile';
import { listDirectoryTool } from './builtins/listDirectory';
import { globTool } from './builtins/glob';
import { grepTool } from './builtins/grep';
import { writeFileTool } from './builtins/writeFile';
import { editFileTool } from './builtins/editFile';
import { runShellTool } from './builtins/runShell';

export function buildToolRegistry(
  approvals: ApprovalQueue,
  sessionId: string,
): Record<string, Tool> {
  return {
    read_file: readFileTool,
    list_directory: listDirectoryTool,
    glob: globTool,
    grep: grepTool,
    write_file: writeFileTool(approvals, sessionId),
    edit_file: editFileTool(approvals, sessionId),
    run_shell: runShellTool(approvals, sessionId),
  };
}
```

- [ ] **Step 5: 写 `tools.dangerous.test.ts`**

```typescript
// apps/desktop/tests/tools.dangerous.test.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, readFile, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { setWorkspaceRoot } from '../src/main/tools/paths';
import { writeFileTool } from '../src/main/tools/builtins/writeFile';
import { editFileTool } from '../src/main/tools/builtins/editFile';
import { runShellTool } from '../src/main/tools/builtins/runShell';

// mock 一个 approvals：可控决策
function mockApprovals(decision: 'allow' | 'deny' = 'allow') {
  return {
    request: async () => decision,
    // 其余方法用不到，给 noop
    respond: () => {},
    clearSession: () => {},
    hasNoPending: () => true,
  };
}

let root: string;
beforeEach(async () => {
  root = await mkdtemp(join(tmpdir(), 'qiming-p2-d-'));
  setWorkspaceRoot(root);
});
afterEach(async () => {
  setWorkspaceRoot('');
  await rm(root, { recursive: true, force: true });
});

describe('write_file', () => {
  it('allow 时写入文件', async () => {
    const t = writeFileTool(mockApprovals('allow') as never, 's1');
    const r = await t.execute({ path: 'out.txt', content: 'hi' }, {} as never);
    expect(r).toMatchObject({ ok: true });
    expect(await readFile(join(root, 'out.txt'), 'utf-8')).toBe('hi');
  });
  it('deny 时返回错误且不写入', async () => {
    const t = writeFileTool(mockApprovals('deny') as never, 's1');
    const r = await t.execute({ path: 'out.txt', content: 'hi' }, {} as never);
    expect(r).toMatchObject({ ok: false });
    expect(r).toHaveProperty('error', '用户拒绝');
    // 文件不应存在
    await expect(readFile(join(root, 'out.txt'), 'utf-8')).rejects.toThrow();
  });
  it('自动创建父目录', async () => {
    const t = writeFileTool(mockApprovals('allow') as never, 's1');
    await t.execute({ path: 'sub/dir/x.txt', content: 'd' }, {} as never);
    expect(await readFile(join(root, 'sub/dir/x.txt'), 'utf-8')).toBe('d');
  });
});

describe('edit_file', () => {
  it('精确替换所有匹配', async () => {
    await writeFile(join(root, 'e.txt'), 'aaa bbb aaa');
    const t = editFileTool(mockApprovals('allow') as never, 's1');
    const r = await t.execute(
      { path: 'e.txt', oldText: 'aaa', newText: 'X' },
      {} as never,
    );
    expect(r).toMatchObject({ ok: true, replaced: 2 });
    expect(await readFile(join(root, 'e.txt'), 'utf-8')).toBe('X bbb X');
  });
  it('oldText 不匹配时返回错误', async () => {
    await writeFile(join(root, 'e.txt'), 'foo');
    const t = editFileTool(mockApprovals('allow') as never, 's1');
    const r = await t.execute(
      { path: 'e.txt', oldText: 'bar', newText: 'baz' },
      {} as never,
    );
    expect(r).toMatchObject({ ok: false });
  });
});

describe('run_shell', () => {
  it('allow 时执行命令并返回输出', async () => {
    const t = runShellTool(mockApprovals('allow') as never, 's1');
    const cmd = process.platform === 'win32' ? 'echo hello' : 'echo hello';
    const r = (await t.execute({ command: cmd }, {} as never)) as {
      ok: boolean;
      stdout: string;
    };
    expect(r.ok).toBe(true);
    expect(r.stdout.toLowerCase()).toContain('hello');
  });
  it('deny 时不执行', async () => {
    const t = runShellTool(mockApprovals('deny') as never, 's1');
    const r = await t.execute({ command: 'echo x' }, {} as never);
    expect(r).toMatchObject({ ok: false, error: '用户拒绝' });
  });
});
```

- [ ] **Step 6: 跑测试**

Run: `cd apps/desktop && corepack pnpm test -- tools.dangerous`
Expected: PASS（write_file 3 + edit_file 2 + run_shell 2 = 7）。

- [ ] **Step 7: 类型检查 + 全量测试**

Run:
```bash
cd apps/desktop && corepack pnpm exec tsc --noEmit -p tsconfig.node.json
corepack pnpm test
```
Expected: 类型通过；全量测试（含 P2.0-P2.4 新增）全 PASS。

- [ ] **Step 8: Commit**

```bash
git add apps/desktop/src/main/tools/ apps/desktop/tests/tools.dangerous.test.ts
git commit -m "feat(p2.4): 3 危险工具（write/edit/run_shell）+ 注入审批 + 注册表启用"
```

---

## Task P2.5：run.ts 接入 tool-use 循环（streamText + fullStream）

**Files:**
- Modify: `apps/desktop/src/main/chat/run.ts:104-113`（streamText 加 tools/maxSteps；textStream→fullStream）
- Modify: `apps/desktop/tests/chat.test.ts`（mock fullStream）

**Interfaces:**
- Consumes: `buildToolRegistry(approvals, sessionId)` from `../tools/registry`；`getApprovalQueue()` from `../approval/queue`；`IPC.CHAT_TOOL_CALL`/`CHAT_TOOL_RESULT` from shared
- Produces: `runTurn` 行为变更（含工具调用循环），签名不变

**关键修正（spec §6.2 vs ai@4.3.19）：** TextStreamPart 中 tool-call 用 `part.args`（非 `input`），tool-result 用 `part.result`（非 `output`）。本 task 一律用正确字段名。

- [ ] **Step 1: 改 `run.ts`**

在顶部 import 块追加：

```typescript
import { buildToolRegistry } from '../tools/registry';
import { getApprovalQueue } from '../approval/queue';
```

在 `const result = streamText({...})` 处（当前 104-109 行）替换为：

```typescript
    const approvalQueue = getApprovalQueue();
    const tools = buildToolRegistry(approvalQueue, sessionId);

    const result = streamText({
      model,
      system: memorySystemPrompt ?? undefined,
      messages: contextMessages,
      tools,
      maxSteps: 25,
      abortSignal: controller.signal,
    });
```

把 `for await (const delta of result.textStream) {...}` 块（当前 111-113 行）替换为 fullStream 消费：

```typescript
    for await (const part of result.fullStream) {
      switch (part.type) {
        case 'text-delta':
          win?.webContents.send(IPC.CHAT_DELTA, {
            sessionId,
            delta: part.textDelta,
          });
          break;
        case 'tool-call':
          win?.webContents.send(IPC.CHAT_TOOL_CALL, {
            sessionId,
            callId: part.toolCallId,
            tool: part.toolName,
            args: part.args as Record<string, unknown>,
          });
          break;
        case 'tool-result':
          win?.webContents.send(IPC.CHAT_TOOL_RESULT, {
            sessionId,
            callId: part.toolCallId,
            tool: part.toolName,
            result: part.result,
            ok: !(part.result instanceof Error),
          });
          break;
        case 'error':
          console.error('[p2] streamText error', part.error);
          break;
        default:
          // step-start/step-finish/finish/reasoning 等不推 UI
          break;
      }
    }
```

> **注**：`part.args` 在 tool-call 类型上是参数对象；`part.result` 在 tool-result 类型上是 execute 返回值。两者的 `part.toolCallId` 用于 UI 把结果配对到调用。`part.toolName` 是 `NAME & string`，可直接赋给 `ToolName`（运行时即工具键）。

- [ ] **Step 2: 改 `tests/chat.test.ts`（mock fullStream）**

把现有 `vi.mock('ai', () => ({ streamText: vi.fn() }));` 保留，并在测试体内补一个返回 fullStream 的 mock。在 `import { runTurn } ...` 之后、`describe` 之前加辅助：

```typescript
import { streamText } from 'ai';

/** 构造一个假的 streamText 返回值：含 fullStream（async iter）+ text + usage。 */
function fakeStreamResult(parts: { type: string;[k: string]: unknown }[]) {
  const fullStream = (async function* () {
    for (const p of parts) yield p;
  })();
  return {
    fullStream,
    text: Promise.resolve('final'),
    usage: Promise.resolve({ promptTokens: 5, completionTokens: 3 }),
  };
}
```

把第一个测试（session 不存在）保持不变（mock store getSession 返回 null，streamText 不会被调）。

在 `describe('runTurn', ...)` 内末尾追加一个新测试，验证 fullStream 的 text-delta 会推 CHAT_DELTA、tools/maxSteps 被传入：

```typescript
  it('text-delta 经 CHAT_DELTA 推送且 streamText 收到 tools/maxSteps', async () => {
    const { BrowserWindow } = await import('electron');
    const send = vi.fn();
    vi.mocked(BrowserWindow.getAllWindows).mockReturnValueOnce([
      { webContents: { send } } as never,
    ]);

    const appendMessage = vi.fn((_, role, content) => ({
      id: 'm1',
      sessionId: 's1',
      role,
      content,
      tokens: null,
      kind: 'message' as const,
      createdAt: 0,
    }));
    vi.mocked(createSessionStore).mockReturnValueOnce({
      getSession: vi.fn(() => ({
        id: 's1',
        title: null,
        providerId: 'p1',
        model: 'gpt',
        createdAt: 0,
        updatedAt: 0,
      })),
      appendMessage,
      messages: vi.fn(() => []),
    } as never);

    // buildModel / 检索/压缩 都走真实路径会失败，mock 掉
    vi.doMock // 占位，见下方说明
    // 用模块级 mock（顶部）更稳：见 Step 3
    await runTurn('s1', 'hi');
    expect(streamText).toHaveBeenCalled();
    const callArg = vi.mocked(streamText).mock.calls.at(-1)?.[0] as Record<string, unknown>;
    expect(callArg).toBeDefined();
    expect(callArg.maxSteps).toBe(25);
    expect(callArg.tools).toBeTypeOf('object');
    // send 至少被调过（text-delta 推送）
    expect(send).toHaveBeenCalled();
  });
```

> 由于 `run.ts` 还 import 了 `buildModel`、`retrieveMemories`、`compressMessages` 等，顶部 mock 需补全。**Step 3 给出完整的 mock 顶部块。**

- [ ] **Step 3: 替换 `chat.test.ts` 顶部 mock 块为完整版**

把 `chat.test.ts` 顶部（import 之后、`describe` 之前）的 mock 块整体替换为：

```typescript
vi.mock('electron', () => ({
  BrowserWindow: { getAllWindows: vi.fn(() => []) },
}));
vi.mock('ai', () => ({ streamText: vi.fn() }));
vi.mock('../src/main/store/db', () => ({ getDb: vi.fn(() => ({})) }));
vi.mock('../src/main/store/sessions', () => ({
  createSessionStore: vi.fn(() => ({
    getSession: vi.fn(() => null),
    appendMessage: vi.fn(),
    messages: vi.fn(() => []),
  })),
}));
vi.mock('../src/main/store/providers', () => ({
  createProviderStore: vi.fn(() => ({ get: vi.fn(() => null) })),
}));
// P2.5 新增：mock 掉 run.ts 用到的工具链/记忆/压缩/模型工厂
vi.mock('../src/main/providers/factory', () => ({
  buildModel: vi.fn(async () => ({})),
}));
vi.mock('../src/main/memory/retriever', () => ({
  retrieveMemories: vi.fn(async () => null),
}));
vi.mock('../src/main/memory/extractor', () => ({
  extractAndStore: vi.fn(async () => undefined),
}));
vi.mock('../src/main/context/compressor', () => ({
  compressMessages: vi.fn(),
}));
vi.mock('../src/main/tools/registry', () => ({
  buildToolRegistry: vi.fn(() => ({ read_file: {} })),
}));
vi.mock('../src/main/approval/queue', () => ({
  getApprovalQueue: vi.fn(() => ({ request: vi.fn(), respond: vi.fn() })),
}));
```

并把 Step 2 的"vi.doMock 占位"那两行删掉，直接：

```typescript
vi.mocked(streamText).mockReturnValueOnce(
  fakeStreamResult([
    { type: 'text-delta', textDelta: 'hel' },
    { type: 'text-delta', textDelta: 'lo' },
  ]) as never,
);
```

放在该测试体内 `await runTurn('s1', 'hi')` 之前。

- [ ] **Step 4: 跑 chat 测试**

Run: `cd apps/desktop && corepack pnpm test -- chat`
Expected: 全 PASS（含原 2 个 + 新 1 个）。

- [ ] **Step 5: 类型检查 + 全量测试**

Run:
```bash
cd apps/desktop && corepack pnpm exec tsc --noEmit -p tsconfig.node.json
corepack pnpm test
```
Expected: 全 PASS。

- [ ] **Step 6: Commit**

```bash
git add apps/desktop/src/main/chat/run.ts apps/desktop/tests/chat.test.ts
git commit -m "feat(p2.5): run.ts 接入 tool-use 循环（streamText+tools/maxSteps 25，fullStream 消费）"
```

---

## Task P2.6：审批弹窗 UI（朱砂印意象）

**Files:**
- Create: `apps/desktop/src/renderer/components/ApprovalDialog.tsx`
- Create: `apps/desktop/src/renderer/components/ApprovalDialog.module.css`
- Modify: `apps/desktop/src/renderer/pages/ChatPage.tsx`（挂载 ApprovalDialog）
- Modify: `apps/desktop/src/renderer/stores/chat.ts`（加 pendingApproval 状态 + onApprovalRequest 监听）

**Interfaces:**
- Consumes: `api.tools.onApprovalRequest(cb)`、`api.tools.respondApproval(req, decision)`；`ApprovalRequest`/`ApprovalDecision` from `@qiming/shared`
- Produces: `ApprovalDialog` 组件；chat store 加 `pendingApproval: ApprovalRequest | null`、`respondApproval(decision): Promise<void>`

- [ ] **Step 1: 在 chat store 加 pendingApproval 状态 + 监听**

在 `ChatState` 接口加字段与方法：

```typescript
  /** P2.6：当前待审批请求（null=无）。一次只展示一个；队列后续请求排队等当前解决。 */
  pendingApproval: ApprovalRequest | null;
  respondApproval: (decision: ApprovalDecision) => Promise<void>;
```

顶部 import 补：

```typescript
import type { Session, Message, ProviderConfig, ApprovalRequest, ApprovalDecision } from '@qiming/shared';
```

在 `ensureStreamListeners` 内追加审批监听（与 onDelta/onDone/onError 并列）：

```typescript
  api.tools.onApprovalRequest((req) => {
    // 只展示属于当前活跃会话的审批（其它会话的请求由其当时的 UI 处理；
    // 实际同时只有一个会话在生成，故直接 set）
    if (req.sessionId === get().activeSessionId) {
      set({ pendingApproval: req });
    }
  });
```

在 `create<ChatState>(...)` 初始 state 加 `pendingApproval: null,`，并加 action：

```typescript
  respondApproval: async (decision) => {
    const req = get().pendingApproval;
    if (!req) return;
    set({ pendingApproval: null });
    await api.tools.respondApproval(req, decision);
  },
```

- [ ] **Step 2: 写 `ApprovalDialog.tsx`**

```tsx
// apps/desktop/src/renderer/components/ApprovalDialog.tsx
import { useState } from 'react';
import { useChatStore } from '../stores/chat';
import type { ApprovalRequest, ToolName } from '@qiming/shared';
import styles from './ApprovalDialog.module.css';

const DANGEROUS: ToolName[] = ['write_file', 'edit_file', 'run_shell'];

/**
 * 朱砂印审批弹窗。危险工具执行前弹出。
 *  - 危险图标朱砂色（古风"盖印前需审视"）。
 *  - run_shell 命令强制展开（detail.command 明文）。
 *  - 三按钮：拒绝（默认）/ 允许本次 / 本会话总允许。
 *  - 无超时，一直等用户决策。
 */
export function ApprovalDialog() {
  const req = useChatStore((s) => s.pendingApproval);
  const respond = useChatStore((s) => s.respondApproval);
  const [expanded, setExpanded] = useState(false);

  if (!req) return null;

  const isShell = req.tool === 'run_shell';
  const autoExpand = isShell; // shell 命令强制展开
  const showDetail = expanded || autoExpand;

  return (
    <div className={styles.overlay} role="dialog" aria-modal="true">
      <div className={styles.panel}>
        <div className={styles.head}>
          <span className={styles.seal} aria-hidden>
            朱
          </span>
          <h2 className={styles.title}>朱砂印 · 待审批</h2>
        </div>
        <div className={styles.body}>
          <div className={styles.summary}>
            <span className={styles.toolName}>{req.tool}</span>
            <span className={styles.summaryText}>{req.summary}</span>
          </div>
          {!autoExpand && (
            <button
              type="button"
              className={styles.toggle}
              onClick={() => setExpanded((v) => !v)}
            >
              {showDetail ? '收起详情' : '查看详情'}
            </button>
          )}
          {showDetail && (
            <pre className={styles.detail}>{JSON.stringify(req.detail, null, 2)}</pre>
          )}
          {isShell && (
            <div className={styles.warn}>命令将以下述明文执行，请审视。</div>
          )}
        </div>
        <div className={styles.footer}>
          <button
            type="button"
            className={styles.btnDeny}
            onClick={() => respond('deny')}
            autoFocus
          >
            拒绝
          </button>
          <button
            type="button"
            className={styles.btnAllow}
            onClick={() => respond('allow')}
          >
            允许本次
          </button>
          <button
            type="button"
            className={styles.btnSession}
            onClick={() => respond('allow_session')}
          >
            本会话总允许
          </button>
        </div>
        {/* DANGEROUS 仅用于语义标记，运行时不渲染；保留供 a11y 后续扩展 */}
        <span hidden>{DANGEROUS.join(',')}</span>
      </div>
    </div>
  );
}
```

> **注**：`DANGEROUS` 常量在此处暂作语义占位（hidden span）。reviewer 若认为多余可删，但不影响功能。ApprovalDialog 仅在 `pendingApproval` 非空时由 store 信号驱动弹出。

- [ ] **Step 3: 写 `ApprovalDialog.module.css`（古风令牌）**

```css
/* apps/desktop/src/renderer/components/ApprovalDialog.module.css */
.overlay {
  position: fixed;
  inset: 0;
  background: rgba(60, 58, 54, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  animation: fadeIn var(--duration-fast) var(--ease-brush-in);
}
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.panel {
  background: var(--bg-paper);
  background-image: var(--texture-paper);
  border: var(--border-width-base) solid var(--accent-cinnabar);
  border-radius: var(--radius-base);
  box-shadow: var(--shadow-ink-xl);
  padding: var(--space-6);
  min-width: 380px;
  max-width: 640px;
}

.head {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  margin-bottom: var(--space-4);
}
.seal {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  background: var(--accent-cinnabar);
  color: var(--text-ink-reverse);
  font-family: var(--font-family-cn);
  font-size: var(--font-size-lg);
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-ink-sm);
}
.title {
  font-size: var(--font-size-lg);
  color: var(--text-ink);
  margin: 0;
  letter-spacing: 0.1em;
}

.body {
  color: var(--text-ink);
  margin-bottom: var(--space-5);
}
.summary {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}
.toolName {
  font-family: var(--font-family-cn);
  font-size: var(--font-size-sm);
  color: var(--accent-cinnabar);
  letter-spacing: 0.05em;
}
.summaryText {
  font-size: var(--font-size-base);
}
.toggle {
  margin-top: var(--space-2);
  background: none;
  border: none;
  color: var(--accent-lapis);
  cursor: pointer;
  font-size: var(--font-size-sm);
  padding: 0;
}
.toggle:hover {
  text-decoration: underline;
}
.detail {
  margin-top: var(--space-3);
  padding: var(--space-3);
  background: var(--bg-paper-dark);
  border: var(--border-width-thin) solid var(--border-ancient);
  border-radius: var(--radius-sm);
  font-family: var(--font-family-mono, monospace);
  font-size: var(--font-size-xs);
  color: var(--text-ink);
  white-space: pre-wrap;
  word-break: break-all;
  max-height: 240px;
  overflow: auto;
}
.warn {
  margin-top: var(--space-3);
  font-size: var(--font-size-sm);
  color: var(--accent-cinnabar);
  font-style: italic;
}

.footer {
  display: flex;
  justify-content: flex-end;
  gap: var(--space-3);
}
.footer button {
  font-family: var(--font-family-cn);
  font-size: var(--font-size-sm);
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-sm);
  cursor: pointer;
  border: var(--border-width-thin) solid var(--border-ancient);
  background: var(--bg-paper-dark);
  color: var(--text-ink);
  transition: background var(--duration-fast) var(--ease-brush-in);
}
.footer button:hover {
  background: var(--bg-silk);
}
.btnDeny {
  border-color: var(--accent-cinnabar) !important;
  color: var(--accent-cinnabar) !important;
}
.btnAllow {
  background: var(--accent-cinnabar) !important;
  color: var(--text-ink-reverse) !important;
  border-color: var(--accent-cinnabar) !important;
}
.btnAllow:hover {
  background: var(--accent-cinnabar-hover) !important;
}
.btnSession {
  border-color: var(--accent-gold) !important;
  color: var(--accent-gold) !important;
}
```

- [ ] **Step 4: 在 `ChatPage.tsx` 挂载 ApprovalDialog**

顶部 import 补：

```typescript
import { ApprovalDialog } from '../components/ApprovalDialog';
```

在 `<main className={styles.main}>` 闭合之后、外层 `<div className={styles.layout}>` 闭合之前，追加：

```tsx
      <ApprovalDialog />
```

- [ ] **Step 5: 类型检查 + 构建**

Run:
```bash
cd apps/desktop && corepack pnpm exec tsc --noEmit
corepack pnpm build
```
Expected: 类型通过；electron-vite build 成功（main + preload + renderer 三段都构建）。

> **注**：UI 组件无自动化测试（沿用 P0/P1 约定：UI 手动验证）。本 task 以类型检查 + 构建通过为门禁，端到端验证在 P2.8。

- [ ] **Step 6: Commit**

```bash
git add apps/desktop/src/renderer/components/ApprovalDialog.tsx apps/desktop/src/renderer/components/ApprovalDialog.module.css apps/desktop/src/renderer/pages/ChatPage.tsx apps/desktop/src/renderer/stores/chat.ts
git commit -m "feat(p2.6): 朱砂印审批弹窗 + chat store pendingApproval 状态"
```

---

## Task P2.7：工具调用可视化 + 工作目录设置 + 启动选目录

**Files:**
- Create: `apps/desktop/src/renderer/components/ToolCallView.tsx`
- Create: `apps/desktop/src/renderer/components/ToolCallView.module.css`
- Modify: `apps/desktop/src/renderer/stores/chat.ts`（加 toolCalls + onToolCall/onToolResult 监听）
- Modify: `apps/desktop/src/renderer/pages/ChatPage.tsx`（渲染 ToolCallView 列表）
- Modify: `apps/desktop/src/renderer/pages/SettingsPage.tsx`（加"工作目录"区域）
- Modify: `apps/desktop/src/main/index.ts`（启动时若 workspace 未设，弹目录选择器）
- Modify: `apps/desktop/src/renderer/ipc/client.ts`（注释更新）

**Interfaces:**
- Consumes: `api.tools.onToolCall(cb)`、`api.tools.onToolResult(cb)`、`api.tools.getWorkspace()`、`api.tools.setWorkspace(path)`；`ToolCallEvent`/`ToolResultEvent`/`ToolName`/`ToolRisk` from shared
- Produces: `ToolCallView` 组件；chat store 加 `toolCalls: ToolCallState[]`；SettingsPage 加工作目录区域

- [ ] **Step 1: chat store 加 toolCalls 状态 + 监听**

在 `ChatState` 接口加：

```typescript
  /** P2.7：工具调用流（按到达顺序，含调用与配对的结果） */
  toolCalls: ToolCallState[];
```

并定义类型（接口外或文件顶部 import 之后）：

```typescript
import type {
  Session,
  Message,
  ProviderConfig,
  ApprovalRequest,
  ApprovalDecision,
  ToolCallEvent,
  ToolResultEvent,
} from '@qiming/shared';

export interface ToolCallState {
  callId: string;
  tool: ToolCallEvent['tool'];
  args: Record<string, unknown>;
  /** 结果未到达 = 'pending'；ok=true = 'ok'；ok=false = 'fail' */
  status: 'pending' | 'ok' | 'fail';
  result?: unknown;
  createdAt: number;
}
```

在 `ensureStreamListeners` 内追加（与 onApprovalRequest 并列）：

```typescript
  api.tools.onToolCall((p) => {
    if (p.sessionId !== get().activeSessionId) return;
    const next: ToolCallState = {
      callId: p.callId,
      tool: p.tool,
      args: p.args,
      status: 'pending',
      createdAt: Date.now(),
    };
    set({ toolCalls: [...get().toolCalls, next] });
  });
  api.tools.onToolResult((p) => {
    if (p.sessionId !== get().activeSessionId) return;
    set({
      toolCalls: get().toolCalls.map((tc) =>
        tc.callId === p.callId
          ? { ...tc, status: p.ok ? 'ok' : 'fail', result: p.result }
          : tc,
      ),
    });
  });
```

初始 state 加 `toolCalls: [],`。在 `selectSession` 与 `newSession` 的 set 块里补 `toolCalls: [],`（切换/新建会话清空）。

- [ ] **Step 2: 写 `ToolCallView.tsx`**

```tsx
// apps/desktop/src/renderer/components/ToolCallView.tsx
import { useState, type ReactNode } from 'react';
import type { ToolCallState } from '../stores/chat';
import styles from './ToolCallView.module.css';

const DANGEROUS = new Set(['write_file', 'edit_file', 'run_shell']);

const STATUS_LABEL: Record<ToolCallState['status'], string> = {
  pending: '⏳ 执行中',
  ok: '✓ 完成',
  fail: '✗ 失败',
};

/**
 * 工具调用折叠卡片。穿插在消息流中渲染工具调用过程。
 *  - 危险工具朱砂色边框，只读青玉色边框。
 *  - 默认折叠详情，点击展开看完整 args/result。
 */
export function ToolCallView({ tc }: { tc: ToolCallState }) {
  const [open, setOpen] = useState(false);
  const dangerous = DANGEROUS.has(tc.tool);
  return (
    <div className={`${styles.card} ${dangerous ? styles.danger : styles.safe}`}>
      <button
        type="button"
        className={styles.head}
        onClick={() => setOpen((v) => !v)}
        aria-expanded={open}
      >
        <span className={styles.status}>{STATUS_LABEL[tc.status]}</span>
        <span className={styles.name}>{tc.tool}</span>
        <span className={styles.argsBrief}>{briefArgs(tc.tool, tc.args)}</span>
        <span className={styles.chevron}>{open ? '▾' : '▸'}</span>
      </button>
      {open && (
        <div className={styles.detail}>
          <Section title="参数">{json(tc.args)}</Section>
          {tc.result !== undefined && (
            <Section title="结果">{json(tc.result)}</Section>
          )}
        </div>
      )}
    </div>
  );
}

function briefArgs(tool: string, args: Record<string, unknown>): string {
  if (tool === 'read_file' || tool === 'list_directory')
    return String(args.path ?? '');
  if (tool === 'write_file')
    return `${args.path ?? ''}`;
  if (tool === 'edit_file')
    return `${args.path ?? ''}`;
  if (tool === 'run_shell') return String(args.command ?? '');
  if (tool === 'glob' || tool === 'grep') return String(args.pattern ?? '');
  return '';
}

function json(v: unknown): string {
  try {
    return JSON.stringify(v, null, 2);
  } catch {
    return String(v);
  }
}

function Section({ title, children }: { title: string; children: ReactNode }) {
  return (
    <div className={styles.section}>
      <div className={styles.sectionTitle}>{title}</div>
      <pre className={styles.pre}>{children}</pre>
    </div>
  );
}
```

- [ ] **Step 3: 写 `ToolCallView.module.css`**

```css
/* apps/desktop/src/renderer/components/ToolCallView.module.css */
.card {
  margin: var(--space-2) 0;
  border: var(--border-width-thin) solid var(--border-ancient);
  border-radius: var(--radius-sm);
  background: var(--bg-paper-dark);
  font-family: var(--font-family-cn);
  overflow: hidden;
}
.safe {
  border-left: var(--border-width-base) solid var(--accent-jade);
}
.danger {
  border-left: var(--border-width-base) solid var(--accent-cinnabar);
}

.head {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  width: 100%;
  padding: var(--space-2) var(--space-3);
  background: none;
  border: none;
  cursor: pointer;
  color: var(--text-ink);
  font-size: var(--font-size-sm);
  text-align: left;
}
.head:hover {
  background: var(--bg-silk);
}
.status {
  flex-shrink: 0;
  font-size: var(--font-size-xs);
}
.name {
  flex-shrink: 0;
  color: var(--accent-lapis);
  font-weight: 600;
}
.argsBrief {
  flex: 1;
  color: var(--text-ink-light);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.chevron {
  flex-shrink: 0;
  color: var(--text-ink-light);
}

.detail {
  padding: var(--space-2) var(--space-3);
  border-top: var(--border-width-thin) solid var(--border-ancient);
}
.section {
  margin-bottom: var(--space-2);
}
.sectionTitle {
  font-size: var(--font-size-xs);
  color: var(--text-ink-light);
  margin-bottom: var(--space-1);
}
.pre {
  margin: 0;
  padding: var(--space-2);
  background: var(--bg-paper);
  border-radius: var(--radius-sm);
  font-family: var(--font-family-mono, monospace);
  font-size: var(--font-size-xs);
  color: var(--text-ink);
  white-space: pre-wrap;
  word-break: break-all;
  max-height: 200px;
  overflow: auto;
}
```

- [ ] **Step 4: 在 `ChatPage.tsx` 渲染 ToolCallView**

import 补：

```typescript
import { ToolCallView } from '../components/ToolCallView';
```

在消息流 `<div className={styles.messages}>` 内、`{s.streaming && (...)}` 块**之前**插入工具调用流（按时间序，仅当前会话）：

```tsx
          {s.toolCalls.map((tc) => (
            <ToolCallView key={tc.callId} tc={tc} />
          ))}
```

- [ ] **Step 5: 在 `SettingsPage.tsx` 加"工作目录 · 案牍之所"区域**

import 补：

```typescript
import { useEffect, useState } from 'react';
```
（已 import useEffect/useState，仅确认）。

在 `<MemoryPanel />` 之前插入工作目录区块。在组件内加状态：

```typescript
  const [workspace, setWorkspace] = useState('');
  useEffect(() => {
    void api.tools.getWorkspace().then(setWorkspace);
  }, []);
```

JSX（在 MemoryPanel 之前）：

```tsx
      <Card className={styles.card}>
        <div className={styles.cardHead}>
          <span className={styles.name}>工作目录 · 案牍之所</span>
        </div>
        <div className={styles.meta}>
          <div>当前：{workspace || '（未设置）'}</div>
          <div className="flex gap-2 mt-2">
            <Button
              variant="primary"
              onClick={async () => {
                // 选目录经 IPC 由 Main 的 dialog 处理（见 Step 6 的 pickWorkspace）
                const path = await api.tools.pickWorkspace();
                if (path) {
                  await api.tools.setWorkspace(path);
                  setWorkspace(path);
                }
              }}
            >
              选择目录
            </Button>
          </div>
          <div className={styles.hint}>
            工作目录用于解析工具调用中的相对路径。仅本地可见，不上传。
          </div>
        </div>
      </Card>
```

**注**：这里用了 `api.tools.pickWorkspace()`——一个**新的 IPC 方法**（Main 用 `dialog.showOpenDialog` 选目录返回路径字符串）。需补到 P2.0 的 `ExposedApi.tools` 与 P2.3 的 handler/preload。**controller 在执行 P2.7 前回 P2.0/P2.3 文件补 `pickWorkspace(): Promise<string | null>`**（或在 P2.7 内一并改 shared/handler/preload）。本 task Step 6/7 给出补丁。

CSS：在 `SettingsPage.module.css` 加（若 `.hint` 不存在）：

```css
.hint {
  margin-top: var(--space-2);
  font-size: var(--font-size-sm);
  color: var(--text-ink-light);
  font-style: italic;
}
```

- [ ] **Step 6: 补 `pickWorkspace` IPC（shared + handler + preload）**

P2.0 `packages/shared/src/ipc.ts`：
- `IPC` 对象加 `TOOLS_PICK_WORKSPACE: 'tools:pick_workspace',`
- `ExposedApi.tools` 加 `pickWorkspace(): Promise<string | null>;`

P2.3 `apps/desktop/src/main/ipc/handlers/tools.ts`：在 `registerToolHandlers` 内追加：

```typescript
import { dialog } from 'electron';
// ... 顶部已有 import { ipcMain }
  ipcMain.handle(IPC.TOOLS_PICK_WORKSPACE, async () => {
    const win = BrowserWindow.getAllWindows()[0];
    const result = await (win
      ? dialog.showOpenDialog(win, { properties: ['openDirectory'] })
      : dialog.showOpenDialog({ properties: ['openDirectory'] }));
    if (result.canceled || result.filePaths.length === 0) return null;
    return result.filePaths[0];
  });
```

handler 文件顶部 import 块需补 `BrowserWindow`（从 electron）与 `IPC.TOOLS_PICK_WORKSPACE`（已 import IPC）：
```typescript
import { ipcMain, dialog, BrowserWindow } from 'electron';
```

P2.3 `apps/desktop/src/preload/index.ts` `tools` 命名空间内追加：
```typescript
    pickWorkspace: () => ipcRenderer.invoke(IPC.TOOLS_PICK_WORKSPACE),
```

- [ ] **Step 7: 改 `main/index.ts` 启动时弹目录选择器（首次）**

在 `app.whenReady().then(...)` 内、`createMainWindow()` 之前插入（settings 已在 registerToolHandlers 里读，但首次为空时需弹窗）：

```typescript
import { app, BrowserWindow, dialog } from 'electron';
import { createMainWindow } from './window/create';
import { registerIpc } from './ipc/register';
import { getDb, createSettingsStore } from './store';
import { setWorkspaceRoot } from './tools/paths';

app.whenReady().then(async () => {
  getDb();
  registerIpc();
  // P2.7：首次启动 workspace 未设时弹目录选择器
  const settings = createSettingsStore(getDb());
  if (!settings.get('workspace')) {
    const result = await dialog.showOpenDialog({ properties: ['openDirectory'] });
    if (!result.canceled && result.filePaths.length > 0) {
      const p = result.filePaths[0];
      settings.set('workspace', p);
      setWorkspaceRoot(p);
    }
  }
  createMainWindow();
  // ... 既有 activate 处理
});
```

> **注**：`registerIpc` 内的 tools handler 已经会在注册时把已存的 workspace 灌进 paths 模块。本处只是补"首次为空则弹窗"。原 `registerIpc()` 调用顺序不变（仍在 getDb 之后）。

- [ ] **Step 8: 更新 `renderer/ipc/client.ts` 注释**

把注释里"当前阶段不可调用 `window.qiming.window.*`"等过时描述更新，加一句"P2 起可用 `api.tools.*`"。仅注释，不改逻辑。

- [ ] **Step 9: 类型检查 + 构建**

Run:
```bash
cd apps/desktop && corepack pnpm exec tsc --noEmit
corepack pnpm build
```
Expected: 类型通过；构建成功。

- [ ] **Step 10: Commit**

```bash
git add apps/desktop/src/renderer/components/ToolCallView.tsx apps/desktop/src/renderer/components/ToolCallView.module.css apps/desktop/src/renderer/stores/chat.ts apps/desktop/src/renderer/pages/ChatPage.tsx apps/desktop/src/renderer/pages/SettingsPage.tsx apps/desktop/src/renderer/pages/SettingsPage.module.css apps/desktop/src/renderer/ipc/client.ts apps/desktop/src/main/index.ts apps/desktop/src/main/ipc/handlers/tools.ts apps/desktop/src/preload/index.ts packages/shared/src/ipc.ts
git commit -m "feat(p2.7): 工具调用折叠卡片 + 工作目录设置 + 启动选目录 + pickWorkspace IPC"
```

---

## Task P2.8：端到端手动验证 + 文档收尾

**Files:**
- Create: `docs/p2-verification.md`（手动验证记录）
- Modify: `docs/superpowers/specs/2026-07-06-p2-tools-design.md`（状态：待评审 → 已实现）

**目标**：跑通完整 P2 流程，记录验证结果，确保所有 task 测试全绿。

- [ ] **Step 1: ABI 还原 + 全量测试**

P2.1 曾把 better-sqlite3 切到 Node ABI 跑测试；本步骤先确认全量测试通过，再视情况还原 Electron ABI。

Run:
```bash
cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3 && npm run install
cd G:/qiming-agent/apps/desktop && corepack pnpm test
```
Expected: 全部测试 PASS（含 P0/P1 既有 + P2 新增 paths/settings/readonly/dangerous/approval-queue/chat）。

如要之后在 Electron 内 dev/build：
```bash
cd node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3 && npx node-gyp rebuild --target=30.5.1 --runtime=electron --disturl=https://electronjs.org/headers
```

- [ ] **Step 2: 类型检查 + 构建**

Run:
```bash
cd apps/desktop && corepack pnpm exec tsc --noEmit
corepack pnpm build
```
Expected: 通过。

- [ ] **Step 3: 启动 dev 手动验证（逐项核对 spec §8.2）**

Run: `cd apps/desktop && corepack pnpm dev`

按 spec §8.2 验收清单逐项手动验证，结果记录到 `docs/p2-verification.md`：

```markdown
# P2 端到端验证记录

**日期**：YYYY-MM-DD
**验证人**：controller
**分支**：feat/p2-tools

## 工具能力
- [ ] LLM 调 read_file 读文件，返回内容正确
- [ ] LLM 调 list_directory 列目录
- [ ] LLM 调 glob/grep 搜索
- [ ] read_file 大文件截断（>50000 字符）
- [ ] glob 上限 200、grep 上限 50

## tool-use 循环
- [ ] "读 X 个文件→总结→写入" 多轮链路成功
- [ ] maxSteps=25 到顶优雅终止（构造场景或观察日志）
- [ ] stop 按钮能中断整个循环

## 权限审批
- [ ] 只读工具不弹窗
- [ ] write_file 弹朱砂印审批窗
- [ ] 三选项（拒绝/允许本次/本会话总允许）都生效
- [ ] 本会话总允许后同会话同工具不再弹
- [ ] 拒绝时工具返回错误给 LLM，LLM 继续对话
- [ ] run_shell 命令明文显示（强制展开）

## UI 可视化
- [ ] ToolCallView 在消息流显示（状态/参数/结果）
- [ ] 危险工具朱砂边框、只读青玉边框
- [ ] 设置页工作目录区域可改
- [ ] 首次启动弹目录选择器

## 跨厂商（可选）
- [ ] OpenAI 兼容端点 tool calling 可用
- [ ] Anthropic tool calling 可用
- [ ] Gemini tool calling 可用

## 备注
（记录任何偏差、降级、已知问题）
```

- [ ] **Step 4: 更新 spec 状态**

把 `docs/superpowers/specs/2026-07-06-p2-tools-design.md` 顶部 `- **状态**：待评审` 改为 `- **状态**：已实现（P2.0-P2.8 完成）`。

- [ ] **Step 5: Commit**

```bash
git add docs/p2-verification.md docs/superpowers/specs/2026-07-06-p2-tools-design.md
git commit -m "docs(p2.8): P2 端到端验证记录 + spec 状态更新为已实现"
```

- [ ] **Step 6: 准备交付 controller（合并 gate）**

P2.8 完成后，所有 task 已 commit 到 `feat/p2-tools` 分支。通知 controller：
- 跑一次 `corepack pnpm test` + `tsc --noEmit` + `pnpm build` 三联确认绿
- 用户决定是否本地验证（同 P0/P1 流程：先不合并 → 本机验证 → 合并 main）
- 合并方式：`git merge --no-ff feat/p2-tools` 到 main，删除 feat 分支

---

## 自审记录（plan author self-review）

**1. Spec 覆盖核验**（spec 章节 → task）：
- §2.2 安全模型（只读自动/危险审批/三选项）→ P2.3（queue）+ P2.4（工具调 request）+ P2.6（弹窗）✅
- §2.3 tool-use 循环（tools + maxSteps:25）→ P2.5 ✅
- §2.4 数据流 → P2.5（fullStream）✅
- §3.1 shared/tool.ts 类型 → P2.0 ✅
- §3.2 IPC 通道 → P2.0（声明）+ P2.3（handler）✅
- §4.1 paths 路径解析 → P2.1 ✅
- §4.2 registry 注册表 → P2.2（骨架）+ P2.4（启用危险）✅
- §4.3 4 只读工具（截断/上限）→ P2.2 ✅
- §4.4 3 危险工具（审批要点）→ P2.4 ✅
- §4.5 zod schema → P2.2/P2.4 各工具内 ✅
- §5 审批引擎（queue/sessionAllowed/单例）→ P2.3 ✅
- §6 run.ts 改造（streamText+fullStream）→ P2.5 ✅（修正 input/output → args/result）
- §7.1 ApprovalDialog → P2.6 ✅
- §7.2 ToolCallView → P2.7 ✅
- §7.3 chat store toolCalls → P2.7 ✅
- §7.4 设置页工作目录 → P2.7 ✅
- §7.5 启动选目录 → P2.7 ✅
- §8.2 验收标准 → P2.8 逐项核对 ✅
- §8.3 不做项（MCP/Skill/沙箱/diff/长任务）→ 计划未引入，符合 ✅

**2. 占位符扫描**：无 TBD/TODO/"实现略"。glob.ts 的 `void root` 是显式降级注解（非占位），保留可读性。ApprovalDialog 的 `DANGEROUS` hidden span 是语义占位，已注解。

**3. 类型一致性**：
- `ApprovalDecision` 全链路 `'deny'|'allow'|'allow_session'`（P2.0 定义 → P2.3 queue → P2.4 工具判断 → P2.6 弹窗回传）✅
- `ToolCallEvent`/`ToolResultEvent` 字段 `callId`/`tool`/`args`/`result`/`ok`（P2.0 定义 → P2.5 run.ts 推送 → P2.7 store 消费）✅
- `buildToolRegistry(approvals, sessionId)` 签名（P2.2 骨架 → P2.4 启用 → P2.5 调用）✅
- `respondApproval` 签名（P2.0 `(id, decision)` → P2.3 修正为 `(req, decision)`，P2.3 commit 含 shared 修改，P2.6 调用用 `(req, decision)`）✅
- AI SDK 字段：`part.args`/`part.result`/`part.toolCallId`/`part.toolName`（P2.5 与 ai@4.3.19 TextStreamPart 一致）✅
- zod schema 与 execute 参数解构一致（每个工具 parameters 定义的字段 = execute 解构字段）✅

**4. 依赖顺序**：P2.0 → P2.1（zod/paths/settings）→ P2.2（只读+registry 骨架，占位 ApprovalQueue）→ P2.3（真实 ApprovalQueue + handler + preload，替换占位）→ P2.4（危险工具，启用 registry）→ P2.5（run.ts）→ P2.6（审批 UI）→ P2.7（工具可视化+工作目录）→ P2.8（验证+文档）。每步依赖前序，无环。

**5. 跨 task 修正声明**（实施时需注意）：
- P2.3 把 `respondApproval` 签名从 `(id, decision)` 改为 `(req, decision)`，同步改 shared/ipc.ts（在 P2.3 commit 内）。
- P2.7 引入 `pickWorkspace` IPC，需回填 shared（IPC 常量 + ExposedApi）/handler/preload（在 P2.7 commit 内一并改，已在 P2.7 Step 6 列出）。
